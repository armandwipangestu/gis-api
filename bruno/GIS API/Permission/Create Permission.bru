meta {
  name: Create Permission
  type: http
  seq: 2
}

post {
  url: http://{{host}}:{{port}}/api/admin/permissions
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access-token}}
}

body:json {
  {
    "name": "events-index"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
